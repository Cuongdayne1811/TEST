mixin search()
    form(id="form-search")
        .input-group
            input(
                type = "text",
                placehoder = "Nhập từ khóa tìm kiếm",
                name = "keyword",
                class = "form-control",
                value = keyword
            )
            .input-group-append
                button(
                    class = "btn btn-success",
                    type = "submit"
                ) Tìm